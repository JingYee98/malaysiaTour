class Location{
  String pid, locname, state, description, url, contact, address, imagename;
  double latitude, longitude;
  Location({
    this.pid,
    this.locname,
    this.state,
    this.description,
    this.latitude,
    this.longitude,
    this.url,
    this.contact,
    this.address,
    this.imagename,
  });
}